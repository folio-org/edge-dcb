server:
  port: 8081
spring:
  application:
    name: edge-dcb
  liquibase:
    enabled: false
  main:
    allow-bean-definition-overriding: true
folio:
  tenant:
    validation:
      enabled: false
management:
  endpoints:
    access:
      default: READ_ONLY
logging:
  level:
    org.springframework.web: debug

